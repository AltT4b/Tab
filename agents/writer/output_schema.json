{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Writer Output",
  "description": "Expected structure of a writer role's deliverable.",
  "type": "object",
  "required": ["title", "body"],
  "additionalProperties": false,
  "properties": {
    "title": {
      "type": "string",
      "description": "Title of the written piece."
    },
    "summary": {
      "type": "string",
      "description": "Optional 1–3 sentence summary of the piece."
    },
    "body": {
      "type": "string",
      "description": "Full written content in the configured format (markdown by default)."
    },
    "word_count": {
      "type": "integer",
      "description": "Approximate word count of the body."
    },
    "metadata": {
      "type": "object",
      "additionalProperties": true,
      "description": "Optional key-value pairs (e.g. intended_audience, version)."
    }
  }
}
